import { Button } from '@/components/ui/button';

const SignUp = () => {
  return (
    <div>
      <Button variant='default' color='primary'>
        Sign up
      </Button>
    </div>
  );
};

export default SignUp;
